services:
  - type: web
    name: eecc-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn eecc_server:app --host 0.0.0.0 --port $PORT
    plan: free
